# isNil

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build isNil` to build the library.

## Running unit tests

Run `nx test isNil` to execute the unit tests via [Vitest](https://vitest.dev/).
