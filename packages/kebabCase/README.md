# kebabCase

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build kebabCase` to build the library.

## Running unit tests

Run `nx test kebabCase` to execute the unit tests via [Vitest](https://vitest.dev/).
