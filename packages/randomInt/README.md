# randomInt

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build randomInt` to build the library.

## Running unit tests

Run `nx test randomInt` to execute the unit tests via [Vitest](https://vitest.dev/).
