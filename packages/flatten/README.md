# flatten

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build flatten` to build the library.

## Running unit tests

Run `nx test flatten` to execute the unit tests via [Vitest](https://vitest.dev/).
