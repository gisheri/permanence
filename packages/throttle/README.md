# throttle

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build throttle` to build the library.

## Running unit tests

Run `nx test throttle` to execute the unit tests via [Vitest](https://vitest.dev/).
